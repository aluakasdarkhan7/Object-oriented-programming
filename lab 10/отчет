## 📝 Отчет по Лабораторной работе №10: Работа с файлами и Обработка исключений

### 🎯 Тема работы
Изучение и практическое применение методов работы с **файловыми операциями (ввод/вывод)** и механизма **обработки исключений** в Python для создания устойчивого и надежного кода.

---

### 📋 Анализ представленного файла (`Darkhan(LAB10).py`)

Представленный код разделен на логические секции, демонстрирующие четыре ключевых аспекта: чтение файлов, запись файлов, обработка стандартных исключений и создание пользовательских исключений.

#### 1. Модуль работы с файлами (`file_utils.py` в коде)

Целью этого модуля является инкапсуляция функций для безопасного взаимодействия с файловой системой, включая обработку ошибок, связанных с файлами (`FileNotFoundError` и `IOError`).

| Функция | Назначение | Ключевые особенности |
| :--- | :--- | :--- |
| `read_file(file_path)` | Чтение и вывод содержимого файла. | Использует конструкцию `try...except` для перехвата `FileNotFoundError` (если файл не существует) и `IOError` (ошибки ввода/вывода). |
| `write_to_file(filename, text)` | Запись текста в указанный файл. | Использует режим записи (`'w'`) и `try...except` для обработки возможных `IOError` при записи. |
| `read_from_file(filename)` | Дополнительная функция для чтения и вывода содержимого. | Аналогично `read_file`, но с более простым выводом сообщения об ошибке `FileNotFoundError`. |

#### 2. Модуль обработки исключений (`exceptions_demo.py` в коде)

Этот модуль демонстрирует обработку как стандартных, так и пользовательских ошибок.

##### A. Обработка стандартных исключений (`divide_numbers`)
Функция выполняет деление двух чисел, запрашиваемых у пользователя, и обрабатывает две стандартные критические ошибки:
* **`ValueError`**: Перехватывается, если пользователь вводит нечисловое значение.
* **`ZeroDivisionError`**: Перехватывается при попытке деления на ноль.
* Используются блоки **`else`** (для кода, выполняемого только при отсутствии ошибок) и **`finally`** (для кода, выполняемого в любом случае).

##### B. Пользовательское исключение (`NegativeNumberError`)
* Был создан класс **`NegativeNumberError`**, который наследуется от базового класса `Exception`. Это позволяет создавать собственные типы ошибок для специфических бизнес-правил.
* Функция `check_positive(number)` использует конструкцию **`raise`** для генерации этого исключения, если переданное число меньше нуля.
* В демонстрационном коде (`main.py` части) показано, как перехватывать это пользовательское исключение с помощью блока `except NegativeNumberError`.

---

### 🔑 Ключевые выводы
1.  **Файловый ввод/вывод (I/O):** Продемонстрировано использование контекстного менеджера **`with open(...)`** для безопасной работы с файлами, гарантирующего их автоматическое закрытие.
2.  **Обработка ошибок:** Эффективно использована конструкция **`try-except-else-finally`** для предотвращения аварийного завершения программы при возникновении стандартных ошибок, таких как деление на ноль или неверный ввод.
3.  **Пользовательские исключения:** Успешно реализован механизм создания и использования **собственных типов ошибок**, что позволяет делать код более семантически чистым и упрощает отладку, четко отделяя логические ошибки приложения от системных.
