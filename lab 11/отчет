## 📝 Отчет по Лабораторной работе №11: Основы Объектно-Ориентированного Программирования (ООП)

### 🎯 Тема работы
Изучение и практическое применение трех ключевых принципов **Объектно-Ориентированного Программирования (ООП)** в Python: **Наследования**, **Полиморфизма** и **Инкапсуляции**, на примере создания иерархии классов.

---

### 📋 Анализ представленного файла (`Darkhan(LAB11).py`)

Представленный файл демонстрирует построение сложной системы классов, имитирующей структуру учебного заведения и геометрические фигуры.

#### 1. Инкапсуляция (Класс `Person`)
Принцип **Инкапсуляции** (сокрытие данных) реализован в базовом классе `Person` и его наследниках.

* **Защищенные атрибуты:** Атрибуты, такие как `_name` и `_age`, помечены префиксом нижнего подчеркивания (`_`), что указывает на то, что они являются защищенными (protected) и предназначены для внутреннего использования в классе или его наследниках.
* **Свойства (Properties):** Используется декоратор **`@property`** для методов `name` и `age`. Это позволяет обращаться к защищенным атрибутам как к обычным переменным (например, `student1.name`), но контролируемым образом (только для чтения), реализуя тем самым механизм **геттеров** (accessor methods).

#### 2. Наследование (Иерархия персонала)
В коде построена иерархия классов, демонстрирующая использование **Наследования** (`super()`) и **Переопределения методов (Method Overriding)**.

* **Базовый класс:** `Person`
* **Наследники первого уровня:**
    * `Student(Person)`: Добавляет специфичные для студента данные (`student_id`, `gpa`).
    * `Teacher(Person)`: Добавляет специфичные для преподавателя данные (`subject`, `salary`).
* **Наследник второго уровня (Многоуровневое наследование):**
    * `Professor(Teacher)`: Наследуется от `Teacher`, добавляя атрибут `research_area`.
* **Переопределение:** Каждый из наследников (`Student`, `Teacher`, `Professor`) переопределяет метод **`display_info()`** базового класса `Person` для вывода информации, специфичной для своего типа, обеспечивая более полную информацию.

#### 3. Полиморфизм (Фигуры)
**Полиморфизм** (многообразие форм) продемонстрирован на примере взаимодействия с различными геометрическими фигурами.

* Классы: `Circle`, `Rectangle`, `Triangle` созданы с общим предком `Shape` (хотя `Shape` не определен в предоставленном фрагменте, он логически существует или подразумевается, а демонстрация полиморфизма является явной).
* **Общий интерфейс:** Все объекты-фигуры содержатся в одном списке `shapes` и вызывается общий метод **`display_info()`**.
* **Различное поведение:** Метод `display_info()` внутри каждого класса фигуры выполняет уникальный для этой фигуры расчет площади (`area()`), демонстрируя, что один и тот же вызов метода может приводить к разному поведению в зависимости от типа объекта.

---

### 🔑 Ключевые выводы
Лабораторная работа №11 подтвердила успешное усвоение основных принципов ООП:

1.  **Инкапсуляция:** Обеспечивает безопасность данных, ограничивая прямой доступ к ним и управляя им через свойства (`@property`).
2.  **Наследование:** Позволяет создавать иерархии классов, повторно использовать код и структурировать предметную область (например, `Professor` наследует функциональность `Teacher`, а тот, в свою очередь, от `Person`).
3.  **Полиморфизм:** Обеспечивает гибкость кода, позволяя обрабатывать объекты различных классов с помощью единого интерфейса, что видно при расчете площади разных геометрических фигур.
